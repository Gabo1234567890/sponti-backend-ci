import { ParticipationsModule } from './participations.module';

describe('ParticipationsModule', () => {
  it('should compile', async () => {
    expect(ParticipationsModule).toBeDefined();
  });
});